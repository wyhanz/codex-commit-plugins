interface:
  display_name: "Commit, Push, PR"
  short_description: "Commit changes, push the branch, and create a GitHub pull request."
  default_prompt: "Use $commit-push-pr to commit local changes, push the branch, and create a GitHub pull request."
policy:
  allow_implicit_invocation: false
